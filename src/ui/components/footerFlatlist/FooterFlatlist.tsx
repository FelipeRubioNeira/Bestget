import { StyleSheet, View } from 'react-native'
import React from 'react'

const FooterFlatlist = () => {
    return (
        <View style={footerFlatlistStyles.container}>

        </View>
    )
}

export default FooterFlatlist

const footerFlatlistStyles = StyleSheet.create({

    container: {
        height: 100,
    }

})